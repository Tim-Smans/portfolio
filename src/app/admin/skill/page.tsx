import SkillForm from '@/components/admin/forms/skillForm';
import React, { FC } from 'react';


const ProjectCreatePage: FC = () => {
  
  return (
    <SkillForm isUpdate={false} />
  );
};

export default ProjectCreatePage;